@{
    ViewBag.Title = "Show claims";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

@model IEnumerable<System.Security.Claims.Claim>
    
@foreach (var claim in Model)
{
    <dt>@claim.Type</dt>
    <dd>@claim.Value</dd>
}